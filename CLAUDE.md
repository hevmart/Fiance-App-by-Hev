# H-Queex Control

## UI standards

Before making any change to `templates/index.html`, read `docs/ui-standards.md`.
After making the change, verify it against every rule in that document. If a change
must deviate from a rule there, say so explicitly and explain why — never deviate
silently.
